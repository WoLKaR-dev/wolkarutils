enum RoundedButtonIconPosition { left, right }

enum Device { android, web, ios, windows, macos, linux }

enum ScreenSize { small, regular, large, xlarge, xxlarge }

typedef JSON = Map<String, dynamic>;
